{
  "name": "mean-app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "installall": "cd angular-frontend && npm install && cd .. && npm install",
    "start": "cd angular-frontend && npm run build && cd .. && node ./bin/www",
    "devstart": "cd angular-frontend && npm run build && cd .. && nodemon ./bin/www"
  },
  "dependencies": {
    "cookie-parser": "~1.4.3",
    "debug": "~2.6.9",
    "express": "~4.16.0",
    "http-errors": "~1.6.2",
    "mongoose": "^5.2.17",
    "morgan": "~1.9.0",
    "nodemon": "^1.18.4",
    "pug": "2.0.0-beta11"
  },
  "devDependencies": {
    "argparse": "^1.0.10"
  }
}
